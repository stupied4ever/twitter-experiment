development:
  sessions:
    default:
      database: twitter_dev
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: twitter_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0
